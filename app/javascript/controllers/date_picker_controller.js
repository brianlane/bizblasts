import { Controller } from "@hotwired/stimulus"

// Empty controller placeholder after removing Flatpickr. Kept for future enhancements if needed.

export default class extends Controller {
  static targets = ["input"]

  connect() {
    // No JavaScript enhancement needed; native HTML5 datetime-local is used instead.
  }
} 