<div class="container mx-auto px-4 py-6">
  <div class="mb-6">
    <%= link_to business_manager_job_form_templates_path, class: "inline-flex items-center text-sm text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white" do %>
      <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
      Back to Job Forms
    <% end %>
  </div>

  <div class="bg-white dark:bg-gray-800 shadow rounded-lg overflow-hidden">
    <!-- Header -->
    <div class="px-6 py-4 border-b border-gray-200 dark:border-gray-700">
      <div class="flex justify-between items-start">
        <div>
          <h1 class="text-2xl font-bold text-gray-900 dark:text-white"><%= @job_form_template.name %></h1>
          <% if @job_form_template.description.present? %>
            <p class="mt-1 text-gray-600 dark:text-gray-400"><%= @job_form_template.description %></p>
          <% end %>
          <div class="mt-2 flex items-center space-x-4">
            <span class="px-2 py-1 text-xs font-semibold rounded-full bg-gray-100 dark:bg-gray-700 text-gray-800 dark:text-gray-200">
              <%= @job_form_template.form_type.humanize %>
            </span>
            <% if @job_form_template.active? %>
              <span class="px-2 py-1 text-xs font-semibold rounded-full bg-green-100 text-green-800">Active</span>
            <% else %>
              <span class="px-2 py-1 text-xs font-semibold rounded-full bg-gray-100 text-gray-800">Inactive</span>
            <% end %>
          </div>
        </div>
        <div class="flex space-x-2">
          <%= link_to preview_business_manager_job_form_template_path(@job_form_template), target: "_blank", class: "inline-flex items-center px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700" do %>
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
            </svg>
            Preview
          <% end %>
          <%= link_to edit_business_manager_job_form_template_path(@job_form_template), class: "inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition" do %>
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
            </svg>
            Edit Template
          <% end %>
        </div>
      </div>
    </div>

    <!-- Stats -->
    <div class="px-6 py-4 bg-gray-50 dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700">
      <div class="grid grid-cols-3 gap-4">
        <div>
          <p class="text-sm text-gray-500 dark:text-gray-400">Total Fields</p>
          <p class="text-2xl font-semibold text-gray-900 dark:text-white"><%= @job_form_template.form_fields.length %></p>
        </div>
        <div>
          <p class="text-sm text-gray-500 dark:text-gray-400">Required Fields</p>
          <p class="text-2xl font-semibold text-gray-900 dark:text-white"><%= @job_form_template.required_field_count %></p>
        </div>
        <div>
          <p class="text-sm text-gray-500 dark:text-gray-400">Submissions</p>
          <p class="text-2xl font-semibold text-gray-900 dark:text-white"><%= @submission_count %></p>
        </div>
      </div>
    </div>

    <!-- Form Fields Preview -->
    <div class="px-6 py-4">
      <h2 class="text-lg font-medium text-gray-900 dark:text-white mb-4">Form Fields</h2>

      <% if @job_form_template.form_fields.any? %>
        <div class="space-y-4">
          <% @job_form_template.form_fields.sort_by { |f| f['position'] || 0 }.each do |field| %>
            <div class="border border-gray-200 dark:border-gray-700 rounded-lg p-4">
              <div class="flex justify-between items-start">
                <div>
                  <p class="font-medium text-gray-900 dark:text-white">
                    <%= field['label'] %>
                    <% if field['required'] %>
                      <span class="text-red-500">*</span>
                    <% end %>
                  </p>
                  <% if field['help_text'].present? %>
                    <p class="text-sm text-gray-500 dark:text-gray-400 mt-1"><%= field['help_text'] %></p>
                  <% end %>
                </div>
                <span class="px-2 py-1 text-xs font-semibold rounded-full bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200">
                  <%= field['type'].humanize %>
                </span>
              </div>

              <% if field['type'] == 'select' && field['options'].present? %>
                <div class="mt-2">
                  <p class="text-sm text-gray-500 dark:text-gray-400">Options:</p>
                  <div class="flex flex-wrap gap-1 mt-1">
                    <% field['options'].each do |option| %>
                      <span class="px-2 py-0.5 text-xs bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-300 rounded"><%= option %></span>
                    <% end %>
                  </div>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      <% else %>
        <p class="text-gray-500 dark:text-gray-400 text-center py-8">No fields defined yet. <%= link_to "Edit this template", edit_business_manager_job_form_template_path(@job_form_template), class: "text-blue-600 hover:text-blue-800" %> to add fields.</p>
      <% end %>
    </div>

    <!-- Services Using This Template -->
    <div class="px-6 py-4 border-t border-gray-200 dark:border-gray-700">
      <h2 class="text-lg font-medium text-gray-900 dark:text-white mb-4">Services Using This Template</h2>

      <% if @services_using.any? %>
        <ul class="divide-y divide-gray-200 dark:divide-gray-700">
          <% @services_using.each do |service| %>
            <li class="py-3 flex justify-between items-center">
              <span class="text-gray-900 dark:text-white"><%= service.name %></span>
              <%= link_to "View Service", business_manager_service_path(service), class: "text-blue-600 hover:text-blue-800 text-sm" %>
            </li>
          <% end %>
        </ul>
      <% else %>
        <p class="text-gray-500 dark:text-gray-400 text-center py-4">This template is not assigned to any services yet.</p>
      <% end %>
    </div>
  </div>

  <!-- Danger Zone -->
  <div class="mt-6 bg-white dark:bg-gray-800 shadow rounded-lg overflow-hidden">
    <div class="px-6 py-4 border-b border-gray-200 dark:border-gray-700">
      <h2 class="text-lg font-medium text-red-600">Danger Zone</h2>
    </div>
    <div class="px-6 py-4 flex justify-between items-center">
      <div>
        <p class="text-sm font-medium text-gray-900 dark:text-white">Delete this template</p>
        <p class="text-sm text-gray-500 dark:text-gray-400">Once deleted, this template cannot be recovered.</p>
      </div>
      <% if @submission_count > 0 %>
        <span class="text-sm text-gray-500">Cannot delete - has <%= @submission_count %> submissions</span>
      <% else %>
        <%= button_to business_manager_job_form_template_path(@job_form_template), method: :delete, data: { confirm: "Are you sure you want to delete this template?" }, class: "px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 transition" do %>
          Delete Template
        <% end %>
      <% end %>
    </div>
  </div>
</div>
