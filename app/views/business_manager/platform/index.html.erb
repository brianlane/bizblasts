<% content_for :page_title, "BizBlasts Platform Loyalty" %>

<div class="min-h-screen bg-gray-50">
  <!-- Header -->
  <div class="bg-white shadow">
    <div class="px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center py-6">
        <div>
          <h1 class="text-2xl font-bold text-gray-900">BizBlasts Platform Loyalty</h1>
          <p class="mt-1 text-sm text-gray-500">Earn points by referring businesses to BizBlasts platform</p>
        </div>
        <div class="flex space-x-3">
          <%= link_to "Back to Dashboard", business_manager_dashboard_path, 
                      class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
        </div>
      </div>
    </div>
  </div>

  <div class="px-4 sm:px-6 lg:px-8 py-8">
    <!-- Stats Overview -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
      <div class="bg-white overflow-hidden shadow rounded-lg">
        <div class="p-5">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-indigo-500 rounded-md flex items-center justify-center">
                <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
                </svg>
              </div>
            </div>
            <div class="ml-5 w-0 flex-1">
              <dl>
                <dt class="text-sm font-medium text-gray-500 truncate">Current Points</dt>
                <dd class="text-lg font-medium text-gray-900"><%= @loyalty_summary[:current_points] %></dd>
              </dl>
            </div>
          </div>
        </div>
      </div>

      <div class="bg-white overflow-hidden shadow rounded-lg">
        <div class="p-5">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-green-500 rounded-md flex items-center justify-center">
                <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                </svg>
              </div>
            </div>
            <div class="ml-5 w-0 flex-1">
              <dl>
                <dt class="text-sm font-medium text-gray-500 truncate">Referrals Made</dt>
                <dd class="text-lg font-medium text-gray-900"><%= @loyalty_summary[:total_referrals_made] %></dd>
              </dl>
            </div>
          </div>
        </div>
      </div>

      <div class="bg-white overflow-hidden shadow rounded-lg">
        <div class="p-5">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-blue-500 rounded-md flex items-center justify-center">
                <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
            </div>
            <div class="ml-5 w-0 flex-1">
              <dl>
                <dt class="text-sm font-medium text-gray-500 truncate">Qualified Referrals</dt>
                <dd class="text-lg font-medium text-gray-900"><%= @loyalty_summary[:qualified_referrals] %></dd>
              </dl>
            </div>
          </div>
        </div>
      </div>

      <div class="bg-white overflow-hidden shadow rounded-lg">
        <div class="p-5">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-purple-500 rounded-md flex items-center justify-center">
                <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.99 1.99 0 013 12V7a4 4 0 014-4z"></path>
                </svg>
              </div>
            </div>
            <div class="ml-5 w-0 flex-1">
              <dl>
                <dt class="text-sm font-medium text-gray-500 truncate">Points Redeemed</dt>
                <dd class="text-lg font-medium text-gray-900"><%= @loyalty_summary[:total_redeemed] %></dd>
              </dl>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Main Content Grid -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      <!-- Referral Code Section -->
      <div class="lg:col-span-2">
        <div class="bg-white shadow rounded-lg mb-8">
          <div class="px-6 py-4 border-b border-gray-200">
            <h3 class="text-lg font-medium text-gray-900">Your Business Referral Code</h3>
            <p class="mt-1 text-sm text-gray-500">Share this code with other businesses to earn 500 points per referral</p>
          </div>
          <div class="p-6">
            <% if current_business.platform_referral_code.present? %>
              <div class="flex items-center justify-between p-4 bg-gray-50 rounded-lg">
                <div>
                  <div class="text-sm font-medium text-gray-500">Referral Code</div>
                  <div class="text-lg font-mono font-bold text-gray-900 platform-referral-code"><%= current_business.platform_referral_code %></div>
                </div>
                <button onclick="copyReferralCode()" class="inline-flex items-center px-3 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 cursor-pointer">
                  <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
                  </svg>
                  Copy
                </button>
              </div>
              <div class="mt-4 p-4 bg-blue-50 rounded-lg">
                <div class="flex">
                  <svg class="w-5 h-5 text-blue-400 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                  <div class="ml-3">
                    <h4 class="text-sm font-medium text-blue-800">How it works</h4>
                    <div class="mt-1 text-sm text-blue-700">
                      <p>When another business uses your referral code during signup, they get <strong>50% off their first month</strong> and you earn <strong>500 BizBlasts points</strong>!</p>
                    </div>
                  </div>
                </div>
              </div>
            <% else %>
              <div class="text-center py-8">
                <div class="max-w-sm mx-auto">
                  <svg class="mx-auto h-16 w-16 text-gray-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.568 3H5.25A2.25 2.25 0 003 5.25v4.318c0 .597.237 1.17.659 1.591l9.581 9.581c.699.699 1.78.872 2.607.33a18.095 18.095 0 005.223-5.223c.542-.827.369-1.908-.33-2.607L11.16 3.66A2.25 2.25 0 009.568 3z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 6h.008v.008H6V6z"></path>
                  </svg>
                  <h3 class="text-lg font-medium text-gray-900 mb-2">No referral code yet</h3>
                  <p class="text-sm text-gray-500 mb-6">Generate your unique referral code to start earning points when other businesses join BizBlasts</p>
                  
                  <%= form_with url: generate_referral_code_business_manager_platform_index_path, method: :post, local: false, id: "generate-code-form", class: "w-full" do |form| %>
                    <%= form.submit "Generate Referral Code", 
                                    class: "w-full inline-flex justify-center items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 shadow-lg transition-colors duration-200 cursor-pointer",
                                    data: { loading_text: "Generating..." } %>
                  <% end %>
                  
                  <div class="mt-4 text-xs text-gray-400">
                    <p>🎯 Earn 500 points per qualified referral</p>
                    <p>💰 Referred businesses get 50% off first month</p>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>

        <!-- Recent Activity -->
        <div class="bg-white shadow rounded-lg">
          <div class="px-6 py-4 border-b border-gray-200">
            <div class="flex items-center justify-between">
              <h3 class="text-lg font-medium text-gray-900">Recent Activity</h3>
              <%= link_to "View All", transactions_business_manager_platform_index_path, 
                          class: "text-sm font-medium text-indigo-600 hover:text-indigo-500 cursor-pointer" %>
            </div>
          </div>
          <div class="flow-root">
            <% if @recent_transactions.any? %>
              <ul class="divide-y divide-gray-200">
                <% @recent_transactions.each do |transaction| %>
                  <li class="px-6 py-4">
                    <div class="flex items-center space-x-4">
                      <div class="flex-shrink-0">
                        <% if transaction.earned? %>
                          <div class="w-8 h-8 bg-green-100 rounded-full flex items-center justify-center">
                            <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                            </svg>
                          </div>
                        <% else %>
                          <div class="w-8 h-8 bg-red-100 rounded-full flex items-center justify-center">
                            <svg class="w-5 h-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 12H4"></path>
                            </svg>
                          </div>
                        <% end %>
                      </div>
                      <div class="flex-1 min-w-0">
                        <p class="text-sm font-medium text-gray-900 truncate">
                          <%= transaction.description %>
                        </p>
                        <p class="text-sm text-gray-500">
                          <%= transaction.created_at.strftime('%m/%d/%Y %I:%M %p') %>
                        </p>
                      </div>
                      <div class="flex-shrink-0 text-right">
                        <div class="text-sm font-medium <%= transaction.earned? ? 'text-green-600' : 'text-red-600' %>">
                          <%= transaction.display_amount %> points
                        </div>
                      </div>
                    </div>
                  </li>
                <% end %>
              </ul>
            <% else %>
              <div class="px-6 py-12 text-center">
                <p class="text-sm text-gray-500">No activity yet. Start referring businesses to earn points!</p>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Sidebar -->
      <div class="space-y-6">
        <!-- Point Redemption -->
        <div class="bg-white shadow rounded-lg">
          <div class="px-6 py-4 border-b border-gray-200">
            <h3 class="text-lg font-medium text-gray-900">Redeem Points</h3>
            <p class="mt-1 text-sm text-gray-500">100 points = $10 off subscription</p>
          </div>
          <div class="p-6">
            <% if @redemption_options.any? %>
              <div class="space-y-3">
                <% @redemption_options.each do |option| %>
                  <div class="flex items-center justify-between p-3 border rounded-lg hover:bg-gray-50">
                    <div>
                      <div class="text-sm font-medium text-gray-900"><%= option[:description] %></div>
                      <div class="text-xs text-gray-500"><%= option[:points] %> points</div>
                    </div>
                    <%= form_with url: redeem_points_business_manager_platform_index_path, method: :post, local: false do |form| %>
                      <%= form.hidden_field :points_amount, value: option[:points] %>
                      <%= form.submit "Redeem", 
                                      class: "inline-flex items-center px-3 py-1 border border-transparent text-xs font-medium rounded text-white bg-indigo-600 hover:bg-indigo-700 cursor-pointer",
                                      data: { 
                                        confirm: "Redeem #{option[:points]} points for #{option[:description]}?",
                                        loading_text: "Redeeming..."
                                      } %>
                    <% end %>
                  </div>
                <% end %>
              </div>
            <% else %>
              <div class="text-center py-6">
                <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
                </svg>
                <h3 class="mt-2 text-sm font-medium text-gray-900">Not enough points</h3>
                <p class="mt-1 text-sm text-gray-500">You need at least 100 points to redeem rewards.</p>
              </div>
            <% end %>
          </div>
        </div>

        <!-- Discount Codes -->
        <div class="bg-white shadow rounded-lg">
          <div class="px-6 py-4 border-b border-gray-200">
            <div class="flex items-center justify-between">
              <h3 class="text-lg font-medium text-gray-900">Your Discount Codes</h3>
              <%= link_to "View All", discount_codes_business_manager_platform_index_path, 
                          class: "text-sm font-medium text-indigo-600 hover:text-indigo-500 cursor-pointer" %>
            </div>
          </div>
          <div class="flow-root">
            <% if @discount_codes.any? %>
              <ul class="divide-y divide-gray-200">
                <% @discount_codes.each do |code| %>
                  <li class="px-6 py-4">
                    <div class="flex items-center justify-between">
                      <div class="flex-1 min-w-0">
                        <p class="text-sm font-mono font-medium text-gray-900 truncate">
                          <%= code.code %>
                        </p>
                        <p class="text-xs text-gray-500">
                          <%= code.display_discount %> • <%= code.status.humanize %>
                        </p>
                      </div>
                      <button onclick="copyDiscountCode('<%= code.code %>')" class="text-xs text-indigo-600 hover:text-indigo-500 cursor-pointer">
                        Copy
                      </button>
                    </div>
                  </li>
                <% end %>
              </ul>
            <% else %>
              <div class="px-6 py-12 text-center">
                <p class="text-sm text-gray-500">No discount codes yet. Redeem points to create discount codes!</p>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
function copyReferralCode() {
  // Get the code from the DOM element instead of ERB to avoid escaping issues
  const codeElement = document.querySelector('.platform-referral-code');
  const code = codeElement ? codeElement.textContent.trim() : '';
  
  if (code && code !== '') {
    // Check if clipboard API is available
    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(code).then(function() {
        showToast('Referral code copied to clipboard!', 'success');
      }).catch(function() {
        // Fallback to legacy method
        fallbackCopyToClipboard(code);
      });
    } else {
      // Use fallback for older browsers
      fallbackCopyToClipboard(code);
    }
  } else {
    showToast('No referral code to copy', 'error');
  }
}

function fallbackCopyToClipboard(text) {
  const textArea = document.createElement('textarea');
  textArea.value = text;
  textArea.style.position = 'fixed';
  textArea.style.left = '-999999px';
  textArea.style.top = '-999999px';
  document.body.appendChild(textArea);
  textArea.focus();
  textArea.select();
  
  try {
    document.execCommand('copy');
    showToast('Referral code copied to clipboard!', 'success');
  } catch (err) {
    showToast('Failed to copy code. Please try selecting and copying manually.', 'error');
  }
  
  document.body.removeChild(textArea);
}

function copyDiscountCode(code) {
  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(code).then(function() {
      showToast('Discount code copied to clipboard!', 'success');
    }).catch(function() {
      fallbackCopyToClipboard(code);
    });
  } else {
    fallbackCopyToClipboard(code);
  }
}

function showToast(message, type) {
  // Create toast notification
  const toast = document.createElement('div');
  toast.className = `fixed top-4 right-4 p-4 rounded-md shadow-lg z-50 ${type === 'success' ? 'bg-green-500' : 'bg-red-500'} text-white`;
  toast.textContent = message;
  document.body.appendChild(toast);
  
  // Remove after 3 seconds
  setTimeout(() => {
    document.body.removeChild(toast);
  }, 3000);
}

// Handle form submissions with loading states
document.addEventListener('DOMContentLoaded', function() {
  const forms = document.querySelectorAll('form[data-remote="true"]');
  forms.forEach(form => {
    form.addEventListener('ajax:before', function() {
      const submitBtn = form.querySelector('[type="submit"]');
      if (submitBtn) {
        submitBtn.disabled = true;
        submitBtn.textContent = submitBtn.dataset.loadingText || 'Loading...';
      }
    });
    
    form.addEventListener('ajax:complete', function() {
      const submitBtn = form.querySelector('[type="submit"]');
      if (submitBtn) {
        submitBtn.disabled = false;
        submitBtn.textContent = submitBtn.value;
      }
    });
    
    form.addEventListener('ajax:success', function(event) {
      const response = event.detail[0];
      if (response.success) {
        showToast(response.message, 'success');
        if (response.referral_code) {
          location.reload(); // Reload to show the new referral code
        }
      }
    });
    
    form.addEventListener('ajax:error', function(event) {
      const response = event.detail[0];
      showToast(response.error || 'An error occurred', 'error');
    });
  });
});
</script> 