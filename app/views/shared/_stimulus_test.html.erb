<%# Test partial for demonstrating Stimulus auto-discovery %>
<%# This can be included in any view to test the hello controller %>

<% if Rails.env.development? %>
  <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-4" data-controller="hello">
    <h3 class="text-lg font-semibold text-blue-800 mb-2">🎯 Stimulus Auto-Discovery Test</h3>
    <p class="text-blue-600 mb-3">This demonstrates auto-discovery of the "hello" controller.</p>
    
    <div class="space-y-2">
      <div>
        <label class="block text-sm font-medium text-blue-700">Your Name:</label>
        <input 
          data-hello-target="name" 
          type="text" 
          placeholder="Enter your name"
          class="mt-1 block w-full px-3 py-2 border border-blue-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500"
        >
      </div>
      
      <div class="flex space-x-2">
        <button 
          data-action="click->hello#greet"
          class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
        >
          Say Hello
        </button>
        
        <button 
          data-action="click->hello#clear"
          class="px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-400"
        >
          Clear
        </button>
      </div>
      
      <div 
        data-hello-target="output"
        class="mt-3 p-3 bg-green-50 border border-green-200 rounded text-green-800 font-medium min-h-[2rem]"
      ></div>
    </div>
    
    <p class="text-xs text-blue-500 mt-3">
      💡 Check the browser console to see auto-discovery logs and controller connection messages.
    </p>
  </div>
<% end %> 