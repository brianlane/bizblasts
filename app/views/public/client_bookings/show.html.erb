<div class="max-w-3xl mx-auto">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-bold">Booking Details</h1>
    <%= link_to "← Back to My Bookings", tenant_my_bookings_path, class: "text-blue-600 hover:underline" %>
  </div>

  <div class="bg-white shadow rounded-lg overflow-hidden mb-6">
    <div class="p-6">
      <%= render "shared/booking_details", 
                 booking: @booking, 
                 show_actions: true, 
                 admin_mode: false,
                 show_customer: true,
                 condensed: false %>
    </div>
  </div>
</div>
