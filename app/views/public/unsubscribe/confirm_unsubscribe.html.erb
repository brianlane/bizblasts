<div class="min-h-screen bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
  <div class="sm:mx-auto sm:w-full sm:max-w-md">
    <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
      <div class="text-center">
        <h2 class="text-2xl font-bold text-gray-900 mb-4">
          Unsubscribe from Emails
        </h2>
        
        <div class="mb-6">
          <% if @email_type.present? %>
            <p class="text-gray-600">
              Are you sure you want to unsubscribe <strong><%= @recipient.email %></strong> from <strong><%= @email_type.humanize %></strong> emails?
            </p>
            <p class="text-sm text-gray-500 mt-2">
              You'll still receive other types of emails, including important transactional emails like booking confirmations and payment receipts.
            </p>
          <% else %>
            <p class="text-gray-600">
              Are you sure you want to unsubscribe <strong><%= @recipient.email %></strong> from all marketing emails?
            </p>
            <p class="text-sm text-gray-500 mt-2">
              You'll still receive important transactional emails like booking confirmations and payment receipts.
            </p>
          <% end %>
        </div>

        <div class="flex flex-col space-y-3">
          <% unsubscribe_params = { token: params[:token] } %>
          <% unsubscribe_params[:type] = @email_type if @email_type.present? %>
          
          <%= button_to "Yes, Unsubscribe Me", unsubscribe_path(unsubscribe_params), 
              method: :patch, 
              class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500",
              data: { confirm: "Are you sure you want to unsubscribe?" } %>
          
          <%= link_to "No, Keep Me Subscribed", root_path, 
              class: "w-full flex justify-center py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
        </div>
      </div>
    </div>
  </div>
</div> 