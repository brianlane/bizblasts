<%# View for public/invoices#index %>
<%# Accessed via tenant subdomain: e.g., http://mybiz.lvh.me:3000/invoices %>

<% @business = current_tenant %>

<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6">My Invoices at <%= @business.name %></h1>

  <div class="bg-blue-100 border-l-4 border-blue-500 text-blue-700 p-4" role="alert">
    <p class="font-bold">Invoices have moved!</p>
    <p class="mb-4">You can now view all your invoices and orders in one place through our unified transactions view.</p>
    <%= link_to 'View My Transactions', tenant_transactions_path(filter: 'invoices'), 
        class: "bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
  </div>

  <%# Placeholder for listing invoices %>
  <%# <% if @invoices.any? %>
    <%# <% @invoices.each do |invoice| %> %>
      <%# Link to tenant_invoice_path(invoice) %>
    <%# <% end %> %>
  <%# <% else %>
    <%# <p>You have no invoices.</p> %>
  <%# <% end %> %>
</div> 