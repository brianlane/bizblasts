VIDEO MEETING CREATION FAILED
=============================

Action Required: A video meeting could not be automatically created for a booking. The customer has been notified of the booking, but no video meeting link was included.

BOOKING DETAILS
---------------
Customer: <%= @customer&.full_name || 'Unknown' %>
Service: <%= @service&.name || 'Unknown' %>
<% if @staff_member %>
Staff Member: <%= @staff_member.name %>
<% end %>
Date: <%= @booking.local_start_time&.strftime("%A, %B %d, %Y") %>
Time: <%= @booking.local_start_time&.strftime("%I:%M %p") %> - <%= @booking.local_end_time&.strftime("%I:%M %p") %>
Booking ID: #<%= @booking.id %>

<% if @error_messages.present? %>
ERROR DETAILS
-------------
<% @error_messages.each do |error| %>
- <%= error %>
<% end %>
<% end %>

WHAT YOU CAN DO
---------------
1. Check that the staff member (<%= @staff_member&.name || 'assigned staff' %>) has a connected video meeting account in Settings > Integrations
2. Verify the video meeting provider (Zoom or Google Meet) is properly configured
3. Create a video meeting manually and share the link with the customer

Check your integrations settings:
<%= tenant_url_for(@business, '/manage/settings/integrations') %>

--
This is an automated notification from <%= @business.name %>.
If you have questions, contact support at <%= @support_email %>.
