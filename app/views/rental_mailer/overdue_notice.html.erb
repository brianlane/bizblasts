<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { text-align: center; padding: 20px 0; border-bottom: 2px solid #ef4444; }
    .header h1 { color: #dc2626; margin: 0; }
    .alert-badge { background: #fef2f2; border: 2px solid #ef4444; color: #dc2626; padding: 15px 30px; border-radius: 8px; text-align: center; margin: 20px 0; }
    .content { padding: 30px 0; }
    .booking-details { background: #f8fafc; padding: 20px; border-radius: 8px; margin: 20px 0; }
    .detail-row { display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #e2e8f0; }
    .footer { text-align: center; padding: 20px 0; border-top: 1px solid #e2e8f0; color: #64748b; font-size: 14px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1><%= @business.name %></h1>
    </div>
    
    <div class="content">
      <div class="alert-badge">
        <h2>⚠️ RENTAL OVERDUE</h2>
        <p style="margin: 10px 0 0 0;">Please return your rental immediately!</p>
      </div>
      
      <p>Hi <%= @customer.first_name %>,</p>
      <p><strong>Your rental is now overdue.</strong> Please return the item as soon as possible to avoid additional late fees.</p>
      
      <div class="booking-details">
        <h3><%= @rental.name %></h3>
        <div class="detail-row">
          <span>Was Due</span>
          <span style="font-weight: bold; color: #dc2626;"><%= @rental_booking.local_end_time&.strftime('%B %d, %Y at %I:%M %p') %></span>
        </div>
        <% if @business.rental_late_fee_enabled? %>
          <div class="detail-row">
            <span>Late Fee Rate</span>
            <span><%= @business.rental_late_fee_percentage || 15 %>% of daily rate per day</span>
          </div>
        <% end %>
      </div>
      
      <p>Late fees may be deducted from your security deposit. Please contact us immediately if you are experiencing any issues.</p>
      
      <% if @rental_booking.location.present? %>
        <h3>📍 Return Location</h3>
        <p>
          <%= @rental_booking.location.name %><br>
          <%= @rental_booking.location.address %><br>
          <%= @rental_booking.location.city %>, <%= @rental_booking.location.state %> <%= @rental_booking.location.zip %>
        </p>
      <% end %>
    </div>
    
    <div class="footer">
      <p>Contact us immediately at <%= @business.email %> or <%= @business.phone %></p>
    </div>
  </div>
</body>
</html>

