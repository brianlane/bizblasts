<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { text-align: center; padding: 20px 0; border-bottom: 2px solid #3b82f6; }
    .header h1 { color: #1e40af; margin: 0; }
    .content { padding: 30px 0; }
    .booking-details { background: #f8fafc; padding: 20px; border-radius: 8px; margin: 20px 0; }
    .booking-details h3 { margin-top: 0; color: #1e40af; }
    .detail-row { display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #e2e8f0; }
    .detail-row:last-child { border-bottom: none; }
    .detail-label { color: #64748b; }
    .detail-value { font-weight: 600; }
    .cta-button { display: inline-block; padding: 12px 24px; background: #3b82f6; color: white; text-decoration: none; border-radius: 6px; margin: 20px 0; }
    .alert { background: #fef3c7; border: 1px solid #f59e0b; padding: 15px; border-radius: 6px; margin: 20px 0; }
    .footer { text-align: center; padding: 20px 0; border-top: 1px solid #e2e8f0; color: #64748b; font-size: 14px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1><%= @business.name %></h1>
    </div>
    
    <div class="content">
      <h2>Rental Booking Received</h2>
      <p>Hi <%= @customer.first_name %>,</p>
      <p>Thank you for your rental booking! Here are your booking details:</p>
      
      <div class="booking-details">
        <h3><%= @rental.name %></h3>
        <div class="detail-row">
          <span class="detail-label">Booking Number</span>
          <span class="detail-value"><%= @rental_booking.booking_number %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Pickup</span>
          <span class="detail-value"><%= @rental_booking.local_start_time&.strftime('%B %d, %Y at %I:%M %p') %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Return</span>
          <span class="detail-value"><%= @rental_booking.local_end_time&.strftime('%B %d, %Y at %I:%M %p') %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Duration</span>
          <span class="detail-value"><%= @rental_booking.duration_display %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Quantity</span>
          <span class="detail-value"><%= @rental_booking.quantity %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Total Amount</span>
          <span class="detail-value"><%= number_to_currency(@rental_booking.total_amount) %></span>
        </div>
        <% if @rental_booking.security_deposit_amount.to_d > 0 %>
          <div class="detail-row">
            <span class="detail-label">Security Deposit</span>
            <span class="detail-value"><%= number_to_currency(@rental_booking.security_deposit_amount) %></span>
          </div>
        <% end %>
      </div>
      
      <% if @rental_booking.status_pending_deposit? %>
        <div class="alert">
          <strong>⚠️ Action Required:</strong> Please pay the security deposit to confirm your booking.
        </div>
        <p style="text-align: center;">
          <a href="<%= tenant_rental_booking_url(@rental_booking, host: @business.public_url) %>" class="cta-button">Pay Deposit Now</a>
        </p>
      <% end %>
      
      <% if @rental_booking.location.present? %>
        <h3>Pickup Location</h3>
        <p>
          <%= @rental_booking.location.name %><br>
          <%= @rental_booking.location.address %><br>
          <%= @rental_booking.location.city %>, <%= @rental_booking.location.state %> <%= @rental_booking.location.zip %>
        </p>
      <% end %>
    </div>
    
    <div class="footer">
      <p>If you have any questions, please contact us at <%= @business.email %>.</p>
      <p>&copy; <%= Date.current.year %> <%= @business.name %></p>
    </div>
  </div>
</body>
</html>

